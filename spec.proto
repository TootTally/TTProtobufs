syntax = "proto3";

package TTSpecProtobuf;

message SongInfo {
    string trackRef = 1;
    int64 songID = 2;
    float gameSpeed = 3;
    float scrollSpeed = 4;
}

message FrameData {
    float noteHolder = 1;
    float pointerPosition = 2;
    bool isTooting = 3;
}